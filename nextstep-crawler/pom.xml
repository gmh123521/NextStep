<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.nextstep</groupId>
        <artifactId>nextstep-parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>nextstep-crawler</artifactId>
    <name>nextstep-crawler</name>
    <description>采集中心：爬虫 + 清洗 + 入库</description>

    <dependencies>
        <dependency>
            <groupId>com.nextstep</groupId>
            <artifactId>nextstep-framework</artifactId>
        </dependency>
        <!-- 采集目标数据表所在模块（复用实体与 Mapper 写库） -->
        <dependency>
            <groupId>com.nextstep</groupId>
            <artifactId>nextstep-data-school</artifactId>
        </dependency>
        <dependency>
            <groupId>com.nextstep</groupId>
            <artifactId>nextstep-data-gov</artifactId>
        </dependency>
        <!-- WebClient：非阻塞 HTTP 抓取（离线环境已缓存，复用 ai 模块同款依赖） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webflux</artifactId>
        </dependency>
    </dependencies>
</project>
